apiVersion: 1

providers:
  - name: mlservice
    orgId: 1
    folder: ML Service
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
